# 작업 인수인계 (이어가기) — cache-necromancer v0.1.1

> 이전 세션이 토큰 부족으로 마감되어 이 문서로 인수한다. 새 세션 Claude 가 그대로 이어받아 진행한다.


## 목표

**cache-necromancer v0.1.1 PR #8 머지 + `/cn:config` 인터랙티브 setter 추가 (v0.1.1 범위 안에 포함하기로 결정)**

PR #8: <https://github.com/token-keeper/cache-necromancer/pull/8>

---

## 현재 진행 상황

- [x] **PR #8 (v0.1.1) 7 commits 완료, push 됨**
  - `a777a6a` fix: dry-run/status sid 8자 마스크
  - `ef44e04` feat: CLAUDE_PLUGIN_OPTION_MODE env override
  - `891e2cd` feat: 첫 spawn 시 config.toml 자동 생성
  - `df32ce5` feat: plugin.json userConfig (mode) + v0.1.1 bump
  - `f80f0a8` docs: README + CHANGELOG
  - `02f2e16` fix: 7개 에이전트 리뷰 반영 (D3 뒤집기 + 데몬 진입 try/except + 테스트 격리)
  - `55adf48` fix(plugin): userConfig.mode schema 보정 (title 필수 + enum 미지원)
- [x] 7개 에이전트 내부 코드리뷰 통과
- [x] codex review (`codex review --base main`) 통과 — "no actionable bugs"
- [x] 사용자 환경 실사용 검증 통과 — `/plugin install` → mode 프롬프트 → config.toml 자동 생성 → 다중 세션 자동 추적 → sid 마스킹 모두 정상
- [x] 회귀 테스트 통과 — 214 passed, 1 skipped + env stress test 통과
- [x] **`/cn:config` 인터랙티브 setter 구현 — 완료** (commit `d97b220` + `d63f8aa`)
- [ ] PR #8 머지 (사용자 명시 지시 필요)
- [ ] v0.1.2 후속 작업 (아래 "다음에 할 일" 참조)

---

## 이미 시도한 것

- **성공**: PLAN D3 (env > file → file > env) 뒤집기. 7개 에이전트 리뷰가 env > file 의 사용자 footgun 지적 → 사용자 결정으로 뒤집음. 후속 fix-up commit (`02f2e16`) 으로 반영.
- **성공**: `daemon/__main__.py` 에 `try/except (ValueError, OSError)` 추가 — invalid mode 가 config.toml 에 있을 때 데몬 spawn loop 방지.
- **성공**: marketplace install 검증 실패 → schema fix → 재시도 통과.
- **시도 후 보류**: `codex exec` 로 상세 second opinion 받기 — 4-5분간 응답 없음, background 종료. `codex review` 결과 ("no actionable bugs") 만 사용.

---

## 현재 리스크 / 막힌 지점

### 누적 발견된 안티패턴 (사용자 도그푸딩 중)

1. **`/cn:dry-run` 네이밍 안티패턴** — 일반 컨벤션에서 dry-run 은 "실행 시뮬레이션" 인데 우리 건 단순 "state 출력". `/cn:plan` 또는 `/cn:preview` 가 더 적합. **v0.1.2 후속 후보**.
2. **`/cn:config` 안티패턴 (현재 작업 중)** — `config` 라는 이름은 set/get 양방향 (예: `git config`) 인데 우리 건 read-only + "vi 편집하세요" 안내. 사용자 결정: `AskUserQuestion` 기반 인터랙티브 setter 로 변경.
3. **`/cn:config` 출력의 "데몬 재시작 필요 없음" 안내 부정확** — `refresh_interval_minutes` 만 즉시 반영. `mode`, `imminent_threshold_minutes`, advanced.* 등은 데몬 재시작 필요. 텍스트 정정 필요. **v0.1.2 후속 후보**.
4. **`/cn:status` 와 `/cn:dry-run` 정보 60-70% 겹침** — 책임 분리 의도였지만 실용 가치 차이 미미. dry-run 의 유일 정보는 `command:` 라인 + `cwd:` 라인. **v0.1.2 후속 후보** (통합 또는 status `--verbose`).

### 잠재 리스크

- **`/cn:config` setter 구현이 PR #8 응집성을 약간 깨는 변경**. 사용자가 v0.1.1 에 포함 결정함. 묶어서 진행.
- **머지 후 v0.1.0 사용자가 업그레이드 시 `~/.cache-necromancer/config.toml` 이 이미 있다면 `ensure_config_file` 이 보존**. 기존 사용자 영향 0.
- **다중 세션 추적**: 사용자가 이번 세션에서 알게 됨. cache-necromancer 외 프로젝트 (vdit-ios-sdk, vdit-ios) 세션 자동 등록 — PRD 의도이지만 비용 인지 필요. **README 트러블슈팅에 추가 권장** (v0.1.2 후속).

---

## 다음에 할 일

### 1. `/cn:config` 인터랙티브 setter 구현 — 완료 ✅

`commands/cn:config.md` 를 `AskUserQuestion` 기반 인터랙티브 setter 로 재작성 완료. 4개 키 노출 (`mode` / `refresh_interval_minutes` / `max_refresh_count` / `imminent_threshold_minutes`). `prompt` 는 자주 안 바꾸는 advanced 라 제외. 현재값은 `✓ ` prefix 로 시각화. `Type something` 자동 자유 입력 옵션 활용.

`scripts/cn_config.py` + `tests/scripts/test_cn_config.py` 삭제. pytest 베이스라인 214 → 209 passed (cn_config 5개 테스트 빠짐 — 정상).

도그푸딩 통과: 4개 질문 한 번에 표시 + ✓ prefix + 자유 입력 (후보 외 값) + 부분 변경 (3개 변경 + 1개 그대로) 정상. 데몬 재시작 조건: `refresh_interval_minutes` 외 어떤 키라도 변경 시 재시작.

**Commit**: `d97b220` (setter) + `d63f8aa` (.gitignore `.omx/` + `uv.lock`).

### 2. PR #8 머지

사용자가 명시적으로 "머지해" 라고 지시할 때만 진행. CLAUDE.md 룰. 사용자가 1번 fix 결과 검증 후 머지 의향 명시 받으면:

```bash
gh pr merge 8 --squash --delete-branch    # 또는 --merge, 사용자 선호 확인
git checkout main
git pull
```

### 3. v0.1.2 후속 PR (별도 작업)

이 PR 의 목적: 슬래시 명령 정비 + 누적 안티패턴 정리. **PR #8 머지 후 별도 PR로 진행**.

- **`/cn:dry-run` → `/cn:plan` rename** (`commands/cn:dry-run.md` → `commands/cn:plan.md`, `scripts/cn_dry_run.py` → `scripts/cn_plan.py`, 모든 참조 갱신, README 업데이트)
- **`/cn:status` 와 `/cn:plan` 정보 중복 정리** — 통합 또는 책임 명확화
- **`/cn:config` 의 부정확한 "데몬 재시작 필요 없음" 안내 정정** — 1번 작업으로 자동 해결될 가능성 (인터랙티브 setter 가 자동 재시작 하므로 안내 자체가 불필요)
- **README 트러블슈팅 추가**: "다른 프로젝트 세션이 자동 추적되는 이유 + 정리 방법"
- **`scripts/cn_status.py:67` `is_current` 변수 추출 롤백 검토** — 이전 세션 review 에서 인접 코드 손댐 borderline 으로 flag 됨 (v0.1.1 fix-up 에선 보류)

### 4. v0.2 후속 (별도 spec)

- Phase 4 — 통계/대시보드 (PRD `docs/superpowers/specs/2026-05-12-cache-necromancer-PRD.md` line 287~ 참조)
- 데몬 config hot-reload (polling loop 안에서 주기적 reload) — 현재는 데몬 재시작 필요한 설정이 다수

---

## 관련 파일

이번 작업에 직접 관련된 파일 3개:

- `/Users/i_brody/Documents/GitHub/cache-necromancer/commands/cn:config.md` — 인터랙티브 setter 로 재작성 대상 (현재 `!`python3 ...`` 단순 호출)
- `/Users/i_brody/Documents/GitHub/cache-necromancer/scripts/cn_config.py` — 삭제 예정 (인터랙티브 setter 도입 시 백엔드 불필요)
- `/Users/i_brody/Documents/GitHub/cache-necromancer/docs/superpowers/plans/2026-05-13-v0.1.1-onboarding-polish.md` — PLAN 문서 (결정 사항 D1~D6 + revised D3 명시. Task 2 본문/Task 5.3 의 구 D3 흔적 일부 남아있음 — cosmetic)

---

## 도메인 정보

**개요**: cache-necromancer 는 Claude Code 의 1시간 프롬프트 캐시 TTL 을 헤드레스 CLI (`claude -p --resume`) 로 자동 갱신하는 macOS 플러그인. v0.1.0 alpha released 후 v0.1.1 onboarding polish 진행 중. PRD: `docs/superpowers/specs/2026-05-12-cache-necromancer-PRD.md`, TECH_SPEC: `docs/superpowers/specs/2026-05-12-cache-necromancer-design-v5.md`.

**핵심 컨벤션**:
- Python 3.11+, pytest + freezegun. 의존성 `uv sync --extra dev` (이미 `.venv` 셋업 완료).
- 테스트 실행: `.venv/bin/python -m pytest --tb=short`. 베이스라인 214 passed, 1 skipped.
- 커밋 메시지: `feat:` / `fix:` / `test:` / `docs:` / `chore:` 접두사 + 한글 본문. Co-author 라인은 `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` 으로 통일 (이전 세션 7 commits 모두 이 형식).
- 디버깅 로그 접두사: `BRODY_` (글로벌 CLAUDE.md 룰).

**함정**:
- `/plugin install` 시 plugin.json schema 검증: `userConfig.*.title` 필수 (이전 세션에서 누락으로 install 실패 → fix). `enum` 키 미지원 — valid values 는 description 에 명시.
- 데몬은 시작 시 1회만 `load_config()`. config.toml 편집 후 일부 키는 데몬 재시작 필요.
- `tests/lib/conftest.py` 의 autouse fixture 가 `CLAUDE_PLUGIN_OPTION_MODE` 환경변수를 매 테스트 시작 시 delenv. 새 테스트에서 env 설정하려면 그 뒤에 setenv.

---

## 환경

- **브랜치**: `feature/v0.1.1-onboarding-polish` (origin 에 push 됨, PR #8 열림)
- **베이스**: `main` (`21e1d28`)
- **로컬 HEAD**: `d63f8aa` (가장 최근, `chore: .gitignore 에 .omx/ + uv.lock 추가`)
- **uncommitted**: 없음 (`.omx/` + `uv.lock` 모두 `.gitignore` 처리됨)
- **사용자 환경 도그푸딩 결과 (머지 후 원상복귀 권장)**: `mode=auto`, `refresh_interval=10`, `max_refresh_count=10`, `imminent_threshold=3`. 머지 후 `/cn:config` 로 `hybrid` / `55` 로 원상복귀 권장 (현재 10분마다 자동 fire 비용 발생).
- **GitHub 원격**: `git@github.com:token-keeper/cache-necromancer.git`

---

## 결정 사항

### PLAN 결정 D1~D6 (PR #8 적용)

`docs/superpowers/plans/2026-05-13-v0.1.1-onboarding-polish.md` 의 "결정 사항" 표 참조. 핵심:

- **D1**: dry-run/status 출력 마스크 형식 = `sid_hash[:8]` (8자 hex prefix)
- **D2**: userConfig 노출 = `mode` 1개만
- **D3 (revised after review)**: mode 우선순위 = config.toml > 기본값. 환경변수는 첫 ensure_config_file 템플릿 생성에만 사용. 7개 에이전트 리뷰에서 사용자 footgun 발견 후 뒤집음.
- **D4**: config.toml 자동 생성 = 데몬 진입점에서 한 번. 이미 있으면 절대 덮어쓰지 않음.
- **D5**: plugin.json hooks 키 복구 안 함 (21e1d28 의도 — "Duplicate hooks file detected" 회피)
- **D6**: 마스킹은 표시 경로에서만. `(this)` 매칭 + 실제 fire argv 는 원본 sid_hash 그대로

### 이번 세션 결정

- **D7 (신규)**: `/cn:config` 를 `AskUserQuestion` 기반 인터랙티브 setter 로 변경. **v0.1.1 안에 포함**. 사용자 결정 (옵션 A: "v0.1.1에 포함, 30분 안에 fix 가능"). [규칙화 후보 — 슬래시 명령 네이밍 = 동작 일치 원칙]
- **D8 (신규)**: `/cn:dry-run` → `/cn:plan` rename, `/cn:status` 와 정보 중복 정리는 **v0.1.2 별도 PR**. v0.1.1 응집성 유지.
- **D9 (신규)**: `/cn:config` 노출 키는 4개 (`mode`, `refresh_interval_minutes`, `max_refresh_count`, `imminent_threshold_minutes`). `prompt` 는 advanced 라 제외 (vi 직접 편집 안내). 도그푸딩 중 UX 안티패턴 2개 발견 후 fix: (1) "어떤 설정 변경?" 1차 분기 질문 제거 → 바로 mode 질문부터, (2) 현재값에 `✓ ` 이모지 prefix 로 시각화. 노출 키 후보 5→4 로 줄인 이유: `AskUserQuestion` 최대 4 questions per call 한계 회피 (분할 호출 UX 어색).

---

위 내용 파악하고 **"다음에 할 일" 1번부터 이어가줘**. `commands/cn:config.md` 인터랙티브 setter 구현이 첫 작업이다. 구현 전에:

1. `git log --oneline 21e1d28..HEAD` 로 현재 브랜치 7개 commit 확인
2. `.venv/bin/python -m pytest --tb=short` 으로 214 passed 베이스라인 재확인
3. `openai-codex` 플러그인의 `adversarial-review.md` 패턴 한 번 더 읽고 `commands/cn:config.md` 작성

불명확한 부분은 추측하지 말고 사용자에게 질문해. 특히 setter 의 AskUserQuestion 옵션 목록 (mode 3개 + refresh_interval 4개 외 추가가 필요한지) 은 사용자 확인 후 진행.
