# cache-necromancer v0.4.0 + marketplace 통합 — 인수인계 (이어가기)

## 작업 방식

# 작업 스타일 안내

## 소통
- 코드 수정 전엔 항상 원인과 수정 방향을 먼저 설명하고, 내 허락 받은 뒤 고칠 것
- 추측과 확신 구분. 2번 생각해서 확실한 것만 단정, 추측이면 추측이라고 명시
- 선택지 제시할 땐 본인 추천과 이유를 반드시 함께
- BRODY 주석에 질문이 여러 개라도 하나씩 순서대로 답변
- 오타/빌드 이슈부터 먼저 수정 완료한 뒤 BRODY 질문 답변
- 긴 로그 분석은 subagent(Sonnet)에 위임해서 메인 컨텍스트 절약

## 코드 제안 포맷
- 파일 경로, 라인 번호, 기존 코드, 수정 코드, 필요 시 주석까지 명시
- 100줄 이상이면 파트로 쪼개기. 파트마다 전체 진행률 표시 후 피드백 받고 다음 파트
- 변경사항 확인은 git diff 로. Read 로 전체 파일 다시 읽지 말 것

## 커밋
- 커밋 메시지는 내가 먼저 승인. 승인받은 뒤 Claude 가 git commit 실행
- 커밋 메시지에 Co-Authored-By 라인 넣지 말 것

## 빌드 / 검증
- xcodebuild, fastlane 같은 빌드 검증은 내가 직접 실행. Claude 가 자동으로 돌리지 말 것

### 이번 세션 업데이트

- **재강조 필요**: 이전 세션의 commit 들 (`d19d7ae`, `4dbe3d3`) 메시지에 `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` 라인이 포함됐다. style-preamble 의 "Co-Authored-By 라인 넣지 말 것" 룰 위반. **앞으로 commit 메시지에 Co-Authored-By 라인 넣지 마라.**

## 목표

cache-necromancer 의 **v0.4.0 마일스톤 (설치 UX 개선) 완성** → **token-keeper/plugins marketplace 신설** → **cache-necromancer repo public 공개**. v0.3.11 단독 공개는 안 한다 — release 정책 결정됨.

## 현재 진행 상황

- [x] 단계 1: cache-necromancer repo 정비 (commit `d19d7ae`)
  - `.claude-plugin/marketplace.json` 삭제 (self-marketplace 폐기)
  - `.claude-plugin/plugin.json` version 0.3.9 → 0.3.11
  - README 설치 안내 → `/plugin marketplace add token-keeper/plugins`
  - author `Brody Byun` → `brody424` 12 파일 일괄 치환
- [x] `docs/release-checklist.md` 신설 (commit `4dbe3d3`) + v0.4.0 마일스톤 항목 추가
- [ ] **v0.4.0-① B. `/cn:set` 슬래시 명령 — 다음 작업**
- [ ] v0.4.0-② C. `scripts/setup.py` wizard
- [ ] v0.4.0-③ CHANGELOG + `pyproject.toml`/`plugin.json` version 0.3.11 → 0.4.0
- [ ] 단계 2-pre: history squash (옵션 A) — public 전환 직전
- [ ] 단계 2: public 전환
- [ ] 단계 3: token-keeper/plugins repo 신설
- [ ] 단계 4: marketplace 셋업 (submodule)
- [ ] 단계 5: token-tracker 정리 (self-marketplace 폐기, owner email 노출 정리)
- [ ] 단계 6: cache-necromancer 영어 README

## 이미 시도한 것

- **성공**: `claude-code-guide` subagent 로 Claude Code plugin 의 인터랙티브 setup 메커니즘 검증. 결론 — `userConfig` 만 공식 지원, `SessionStart` hook 의 stdout 은 사용자 chat 에 표시 X, `PluginInstall`/`OnInstall` 류 hook 없음, manifest 에 환영 메시지 필드 없음. → B+C 조합으로 우회 결정.
- **확인**: `fivetaku/gptaku_plugins` 가 monorepo + git submodule 패턴으로 통합 marketplace 운영 중. 같은 패턴 채택.
- **검증**: 한글 README v0.3.11 + 통합 marketplace 설치 안내 반영본이 main 에 있음. 사용자 피드백 픽스 후 영어 README 작성 예정.

## 현재 리스크/막힌 지점

- **v0.4.0-① B 명세 미합의**: `/cn:set` syntax (`/cn:set mode=auto` 추천하지만 미확정) + 지원할 setting key 목록 (`mode`, `refresh_interval_minutes`, `max_refresh_count`, `hybrid_wait_seconds` 4개 후보) + validation 규칙. **구현 전 사용자 확인 필요.**
- **v0.4.0-② C 명세 미합의**: `scripts/setup.py` wizard 흐름 (몇 단계, 묻는 항목) + Python 의존성 (`rich` 추가할지 표준 `input()` 만 쓸지). **구현 전 사용자 확인 필요.**
- 큰 새 기능이라 `superpowers:brainstorming` skill 활용 권장하나, 사용자가 빠른 진행 원할 가능성 — 사용자 의사 먼저 확인하라.

## 다음에 할 일

1. **v0.4.0-① B 명세 합의** — 사용자에게 다음을 짧게 확인하라 (한 번에 여러 결정이라 `AskUserQuestion` 보단 텍스트로 추천안 제시 후 `ㄱㄱ` 받기):
   - `/cn:set <key>=<value>` syntax (추천: equals 형식)
   - 지원 key 4종: `mode`, `refresh_interval_minutes` (또는 짧은 alias `interval`), `max_refresh_count` (`max`), `hybrid_wait_seconds` (`wait`)
   - validation 실패 시 stderr 출력 후 변경 skip
2. **합의 후 구현 (TDD 권장)**:
   - `tests/scripts/test_on_set_command.py` 먼저 작성 (각 key 별 happy path + invalid value)
   - `scripts/on_set_command.py` 신설 — `lib/config.py` 의 atomic save 활용
   - `commands/cn:set.md` 작성 (UserPromptExpansion hook 트리거 — `/cn:status` 의 `commands/cn:status.md` 패턴 참조)
   - `hooks.json` 의 `UserPromptExpansion` 항목 갱신 (단일 hook 안에서 command 분기 vs 분리 hook 결정)
3. **검증**: `uv run pytest` (현재 baseline 131 → 신규 추가 후 ↑). 사용자에게 실 작동 테스트 요청 ("`/cn:set mode=auto` 했을 때 config.toml 변경 + LLM turn 0 확인").
4. **v0.4.0-② C (`scripts/setup.py`) 합의 + 구현** — wizard 흐름 + 의존성 결정 후 진행. 동일 TDD 흐름.
5. **v0.4.0-③ 마무리** — `CHANGELOG.md` v0.4.0 entry, `pyproject.toml` + `.claude-plugin/plugin.json` version bump, PR + 머지.
6. v0.4.0 완성 후 release 단계 2~6 진행.

## 관련 파일

- `docs/release-checklist.md` — 전체 작업 로드맵 + 결정 기록. v0.4.0 마일스톤 + B+C 설계 결정 포함. **이 파일을 항상 먼저 참조하라.**
- `scripts/on_status_command.py` — `/cn:set` 의 hook 참조 패턴. UserPromptExpansion hook 으로 LLM turn 0 달성하는 검증된 구현.
- `lib/config.py` — `config.toml` 파싱/atomic save 라이브러리. `/cn:set` 의 저장 로직에서 그대로 활용.

## 도메인 정보

- 개요: cache-necromancer 는 Claude Code plugin. Stop hook + `asyncRewake` 로 매 50분 chat 세션이 자기 자신을 wake → cache prefix 100% hit. 현재 alpha (v0.3.11).
- 함정: hook 은 silent fail (exit 0) 가 PRD 불변. chat 동작 차단 절대 X. `UserPromptExpansion` hook 의 stdout 이 user prompt 로 expand 되는 메커니즘이 `/cn:status` LLM turn 0 의 핵심 — 같은 패턴이 `/cn:set` 에도 적용된다.
- 컨벤션: 디버깅 로그 prefix `BRODY_`. commit prefix 는 `feat(vX.X.X):` / `fix(vX.X.X):` / `chore:` / `docs:` / `test:` 한글 본문.

## 환경

- repo: `/Users/i_brody/Documents/GitHub/cache-necromancer`
- branch: `main` (origin/main 과 sync)
- repo visibility: **PRIVATE** (단계 2 직전까지 유지)
- pytest baseline: 131/131 (`uv run pytest`)
- 사용 도구: `uv` (의존성 관리), `gh` (GitHub CLI)

## 결정 사항

- **marketplace repo 이름**: `token-keeper/plugins` (사용자 결정)
- **marketplace.json `name` 필드**: `"token-keeper"` — `/plugin install <plugin>@token-keeper` 형태
- **plugin 호스팅**: monorepo + git submodule (fivetaku/gptaku_plugins 패턴)
- **history 정리**: 옵션 A — 전체 squash to 1 commit. private 상태에서 force push. public 전환 직전 실행
- **설치 UX (v0.4.0)**: B (`/cn:set` slash 명령) + C (`scripts/setup.py` shell escape wizard) 조합. 둘 다 LLM turn 0. v0.3.3 의 `cn install` CLI 폐기 결정과는 별개 (외부 CLI 가 아니라 plugin 내 hook + shell escape)
- **release 정책**: v0.3.11 단독 공개 X → v0.4.0 완성 후 marketplace 통합 + 공개

---

위 내용 파악하고 "다음에 할 일" 1번부터 이어가줘. 불명확한 부분은 추측하지 말고 질문해.
