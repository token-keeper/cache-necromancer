# v0.1.1 Onboarding Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v0.1.0 alpha 도그푸딩 첫 라운드에서 발견된 onboarding 마찰 4건을 묶어 v0.1.1로 마감.

**Architecture:**
- `dry-run` / `status` 출력의 session_id 노출을 `sid_hash[:8]` 짧은 마스크로 강화 (transcript 캡처 노출 보호 — CHANGELOG가 약속한 `<sid:abc12345>` 형식 충족).
- Claude Code 공식 `userConfig` (필드 1개: `mode`)로 `/plugin install` 시점에 첫 모드 선택 안내. 환경변수 `CLAUDE_PLUGIN_OPTION_MODE` 가 `config.toml`의 `[general].mode` 를 override (공식 패턴 준수).
- 첫 데몬 spawn 시 `~/.cache-necromancer/config.toml` 이 없으면 기본 템플릿 자동 생성 (사용자가 그 위에 추가 편집할 수 있도록). 이미 있으면 절대 덮어쓰지 않음.

**Tech Stack:** Python 3.11+ (기존 그대로), Claude Code plugin manifest schema (v2.1.140+, `userConfig` 지원).

**Reference Documents:**
- PRD: `docs/superpowers/specs/2026-05-12-cache-necromancer-PRD.md` (line 281: `userConfig 활용한 첫 설치 시 모드 안내` 약속)
- TECH_SPEC v5.1: `docs/superpowers/specs/2026-05-12-cache-necromancer-design-v5.md`
- Claude Code plugin docs: <https://code.claude.com/docs/en/plugins-reference.md#user-configuration>

---

## 결정 사항 (PLAN 단계 확정)

| # | 결정 | 근거 |
|---|---|---|
| D1 | dry-run/status 출력 마스크 형식: `sid_hash[:8]` | CHANGELOG `<sid:abc12345>` 약속 형식과 일치. 8 hex 충돌 확률 매우 낮음. |
| D2 | userConfig 노출 범위: `mode` 1개만 | 비용·동작 영향 최대 1개에 집중. 나머지는 config.toml. PRD 표 4-6개 모두 노출은 옵션 폭주. |
| D3 (revised after review) | mode 우선순위: config.toml > 코드 기본값(`hybrid`). 환경변수는 첫 설치 시 `ensure_config_file`의 템플릿 생성에만 사용 | 7개 에이전트 리뷰에서 사용자 편집 무력화 footgun 발견 → 사용자 결정으로 file 권위로 뒤집음 |
| D4 | config.toml 자동 생성: 데몬 진입점에서 한 번. 이미 있으면 절대 덮어쓰지 않음 | 사용자 편집 보존이 첫 원칙. 첫 spawn 시점 = 첫 Stop hook 직후라 자연스럽게 늦지 않음. |
| D5 | `plugin.json`의 `hooks` 키는 복구하지 않음 | 21e1d28 의도 확정 ("Duplicate hooks file detected" 회피). 자동 로드만 사용. |
| D6 | 마스킹 적용 시 `(this)` 매칭 로직은 마스킹 *전* sid_hash 로 진행 | 표시는 마스크, 비교는 원본. 사용자 경험 깨지지 않음. |

---

## File Structure

| 파일 | 책임 | 변경 |
|---|---|---|
| `lib/mask.py` | `mask_sid(sid_hash) -> str` — 8자 hex prefix 반환 | 신규 |
| `tests/lib/test_mask.py` | `mask_sid` 단위 테스트 | 신규 |
| `lib/config.py` | `load_config` 에서 환경변수 override + `ensure_config_file(root)` 헬퍼 추가 | 수정 |
| `tests/lib/test_config_env_override.py` | 환경변수 우선순위 테스트 | 신규 |
| `tests/lib/test_config_auto_create.py` | `ensure_config_file` 자동 생성/보존 테스트 | 신규 |
| `daemon/__main__.py` | `ensure_config_file` 호출 (load_config 전) | 수정 |
| `scripts/cn_dry_run.py` | `mask_sid` 적용 (헤더 + `--resume` 마스킹) | 수정 |
| `scripts/cn_status.py` | `mask_sid` 적용 (세션 헤더), `(this)` 매칭은 원본 sid_hash로 | 수정 |
| `tests/scripts/test_cn_dry_run.py` | UUID 입력 시 출력에 원본 session_id 없음 검증 | 수정 |
| `tests/scripts/test_cn_status.py` | UUID 입력 시 출력에 원본 session_id 없음 + `(this)` 정상 동작 | 수정 |
| `.claude-plugin/plugin.json` | `userConfig` 필드 추가, `version` `0.1.1` | 수정 |
| `.claude-plugin/marketplace.json` | `version` `0.1.1` | 수정 |
| `pyproject.toml` | `version` `0.1.1` | 수정 |
| `README.md` | 설치 절차 (mode 프롬프트 안내) + 트러블슈팅 한 줄 추가 | 수정 |
| `CHANGELOG.md` | `[0.1.1]` 섹션 (Fixed/Changed/Added) | 수정 |

---

## 작업 원칙

- **TDD**: Red → Green → Refactor.
- **각 Task = 1 의미 단위 commit**. 6개 Task → 6 commit (PR 1개로 묶음).
- **commit 메시지**: `feat:` / `fix:` / `test:` / `docs:` / `chore:` + 한글 설명.
- **검증 마지막**: 사용자가 `/plugin uninstall` → `/plugin install` 사이클로 mode 프롬프트 실제 확인.

---

## Task 1: sid 마스킹 헬퍼 + dry-run/status 적용

**Files:**
- Create: `lib/mask.py`
- Create: `tests/lib/test_mask.py`
- Modify: `scripts/cn_dry_run.py`
- Modify: `scripts/cn_status.py`
- Modify: `tests/scripts/test_cn_dry_run.py`
- Modify: `tests/scripts/test_cn_status.py`

### Step 1.1: 실패 테스트 — `mask_sid`

Create `tests/lib/test_mask.py`:

```python
import pytest
from lib.mask import mask_sid


def test_mask_short_uuid_prefix():
    """36자 UUID → 첫 8자만 노출."""
    sid = "1a6f51ab-49db-4284-84e5-0fc1e951782d"
    assert mask_sid(sid) == "1a6f51ab"


def test_mask_short_id_passthrough_when_le_8():
    """이미 8자 이하면 그대로 (해시 결과 등)."""
    assert mask_sid("abc12345") == "abc12345"
    assert mask_sid("abcd") == "abcd"


def test_mask_empty_returns_question():
    """sentinel '?' 입력 또는 빈 값 → '?' 그대로."""
    assert mask_sid("?") == "?"
    assert mask_sid("") == "?"


def test_mask_is_deterministic():
    assert mask_sid("foobar1234567890") == mask_sid("foobar1234567890")


def test_mask_does_not_leak_full_sid():
    sid = "1a6f51ab-49db-4284-84e5-0fc1e951782d"
    masked = mask_sid(sid)
    # 원본 sid의 8자 초과 부분이 결과에 포함되어선 안 됨
    assert sid[8:] not in masked
```

### Step 1.2: 테스트 실행 — 실패 확인

Run: `pytest tests/lib/test_mask.py -v`

Expected: 5 FAIL (`ImportError: cannot import name 'mask_sid'`).

### Step 1.3: `lib/mask.py` 구현

Create `lib/mask.py`:

```python
"""짧은 sid 마스크 헬퍼.

dry-run / status 출력 등 transcript 캡처 가능 위치에서 원본 session_id
(또는 sanitize 통과한 UUID) 가 그대로 노출되지 않도록 8자 prefix만 표시한다.
파일시스템 / 상태 비교에는 원본 sid_hash 를 그대로 사용한다 — 표시 경로
한정 마스킹.
"""


def mask_sid(sid_hash: str) -> str:
    if not sid_hash:
        return "?"
    if sid_hash == "?":
        return "?"
    if len(sid_hash) <= 8:
        return sid_hash
    return sid_hash[:8]
```

### Step 1.4: 테스트 재실행 — 통과 확인

Run: `pytest tests/lib/test_mask.py -v`

Expected: 5 passed.

### Step 1.5: `cn_dry_run.py` 마스킹 적용

Modify `scripts/cn_dry_run.py`:

기존 import 블록에 추가:

```python
from lib.mask import mask_sid  # noqa: E402
```

기존 `_redact_command` 와 `_format_active` 의 sid 사용 지점 두 곳을 마스킹으로 교체:

```python
def _redact_command(cmd: list[str], sid_hash: str) -> str:
    """`--resume <full-id>` 의 full id를 마스킹해 출력 안전 보장."""
    redacted = list(cmd)
    try:
        idx = redacted.index("--resume")
    except ValueError:
        return shlex.join(redacted)
    if idx + 1 < len(redacted):
        redacted[idx + 1] = f"<sid:{mask_sid(sid_hash)}>"
    return shlex.join(redacted)


def _format_active(s: dict, now: datetime, config) -> list[str]:
    sid = s.get("sid_hash", "?")
    masked = mask_sid(sid)
    lines = [f"  [{masked}]"]
    # ... (이하 기존 로직, build_fire_command 인자는 원본 sid 유지)
    next_at = parse_iso(s.get("next_refresh_at"))
    if next_at is None:
        lines.append("      will fire at: — (next_refresh_at 미설정)")
    else:
        lines.append(
            f"      will fire at: {next_at.isoformat()} "
            f"({_format_delta(next_at, now)})"
        )
    lines.append(f"      mode:         {mode_label(config.mode, config)}")
    argv = build_fire_command(s, config)
    lines.append(f"      command:      {_redact_command(argv, sid)}")
    cwd = s.get("cwd")
    if cwd:
        lines.append(f"      cwd:          {cwd}")
    lines.append(
        f"      refresh_count: {s.get('refresh_count', 0)} / "
        f"{config.max_refresh_count}"
    )
    if s.get("backoff_until"):
        lines.append(f"      backoff_until: {s['backoff_until']}")
    if s.get("consecutive_fire_failures", 0) > 0:
        lines.append(
            f"      consec_fails:  {s['consecutive_fire_failures']}"
        )
    return lines


def _format_disabled(s: dict) -> list[str]:
    sid = s.get("sid_hash", "?")
    masked = mask_sid(sid)
    return [
        f"  [{masked}]  🛑 DISABLED",
        f"      reason:      {s.get('disabled_reason', '?')}",
        f"      disabled_at: {s.get('disabled_at', '?')}",
    ]
```

### Step 1.6: `cn_status.py` 마스킹 적용

Modify `scripts/cn_status.py`:

기존 import 블록에 추가:

```python
from lib.mask import mask_sid  # noqa: E402
```

`_format_session` 의 sid 헤더만 마스킹 (matching 은 원본 `sid_hash` 그대로 유지):

```python
def _format_session(s: dict, now: datetime, current_sid: str | None) -> list[str]:
    sid_hash = s.get("sid_hash", "?")
    is_current = bool(current_sid) and sid_hash == current_sid
    masked = mask_sid(sid_hash)
    marker = " (this)" if is_current else ""
    disabled = s.get("disabled", False)

    lines: list[str] = []
    if disabled:
        lines.append(f"  {masked}{marker}  🛑 DISABLED")
        lines.append(f"      reason:        {s.get('disabled_reason', '?')}")
        lines.append(f"      disabled_at:   {s.get('disabled_at', '?')}")
        return lines

    next_at = parse_iso(s.get("next_refresh_at"))
    last_fire = s.get("last_fire_at")
    last_reason = s.get("last_fire_reason")

    lines.append(f"  {masked}{marker}")
    # (이하 기존 그대로)
    ...
```

(나머지 본문은 변경 없음.)

### Step 1.7: dry-run 출력 테스트 추가

Modify `tests/scripts/test_cn_dry_run.py` — 다음 테스트 함수 1개 추가 (기존 테스트 위치 옆에):

```python
def test_dry_run_masks_uuid_session_id(cn_root, monkeypatch, capsys):
    """UUID 형식 session_id는 출력에 원본 그대로 노출되면 안 된다."""
    import json
    from datetime import datetime, timedelta, timezone
    monkeypatch.setattr("lib.state.STATE_DIR", cn_root / "state")
    sid = "1a6f51ab-49db-4284-84e5-0fc1e951782d"
    now = datetime.now(timezone.utc)
    state_path = cn_root / "state" / f"{sid}.json"
    state_path.write_text(json.dumps({
        "session_id": sid,
        "sid_hash": sid,
        "transcript_path": "/tmp/x.jsonl",
        "cwd": "/tmp",
        "last_stop_at": now.isoformat(),
        "next_refresh_at": (now + timedelta(minutes=2)).isoformat(),
        "refresh_count": 0,
        "consecutive_fire_failures": 0,
        "disabled": False,
    }))

    from scripts import cn_dry_run
    cn_dry_run.main()
    out = capsys.readouterr().out
    # 원본 sid 의 8자 초과 부분이 출력에 포함되면 안 됨
    assert sid[8:] not in out
    # 마스크된 8자 prefix는 보여야 함
    assert "1a6f51ab" in out
```

### Step 1.8: status 출력 테스트 추가

Modify `tests/scripts/test_cn_status.py` — 다음 테스트 함수 1개 추가:

```python
def test_status_masks_uuid_session_id_but_keeps_this_marker(cn_root, monkeypatch, capsys):
    """UUID 세션의 원본 노출 없이 (this) 마커는 정상 표시."""
    import json
    from datetime import datetime, timedelta, timezone
    monkeypatch.setattr("lib.state.STATE_DIR", cn_root / "state")
    sid = "1a6f51ab-49db-4284-84e5-0fc1e951782d"
    monkeypatch.setenv("CLAUDE_CODE_SESSION_ID", sid)
    now = datetime.now(timezone.utc)
    (cn_root / "state" / f"{sid}.json").write_text(json.dumps({
        "session_id": sid,
        "sid_hash": sid,
        "transcript_path": "/tmp/x.jsonl",
        "cwd": "/tmp",
        "last_stop_at": now.isoformat(),
        "next_refresh_at": (now + timedelta(minutes=2)).isoformat(),
        "refresh_count": 0,
        "consecutive_fire_failures": 0,
        "disabled": False,
    }))
    from scripts import cn_status
    cn_status.main()
    out = capsys.readouterr().out
    assert sid[8:] not in out
    assert "1a6f51ab" in out
    assert "(this)" in out  # 매칭은 원본 sid_hash 로 진행되므로 유지
```

### Step 1.9: 전체 테스트 통과 확인 + commit

Run: `pytest tests/lib/test_mask.py tests/scripts/test_cn_dry_run.py tests/scripts/test_cn_status.py -v`

Expected: all passed.

```bash
git add lib/mask.py tests/lib/test_mask.py scripts/cn_dry_run.py scripts/cn_status.py tests/scripts/test_cn_dry_run.py tests/scripts/test_cn_status.py
git commit -m "fix: dry-run/status 출력에서 session_id 8자 마스크 (transcript 노출 보호)"
```

---

## Task 2: config.py 환경변수 override (첫 템플릿 생성 한정)

> **D3 뒤집기 반영 (review fix-up):** `load_config` 는 env var 를 무시. 환경변수는 `ensure_config_file` 의 첫 템플릿 생성에만 사용. `test_config_env_override.py` 는 negative test 로 전환.

**Files:**
- Modify: `lib/config.py`
- Create: `tests/lib/test_config_env_override.py`

### Step 2.1: 실패 테스트

Create `tests/lib/test_config_env_override.py`:

```python
import pytest
from pathlib import Path
from lib.config import load_config


def test_env_var_overrides_default(monkeypatch, tmp_path):
    """CLAUDE_PLUGIN_OPTION_MODE 환경변수가 코드 기본값(hybrid)을 override."""
    monkeypatch.setenv("CLAUDE_PLUGIN_OPTION_MODE", "notify")
    c = load_config(tmp_path / "missing.toml")
    assert c.mode == "notify"


def test_env_var_overrides_file_value(monkeypatch, tmp_path):
    """환경변수가 config.toml 의 mode 도 override."""
    p = tmp_path / "c.toml"
    p.write_text('[general]\nmode = "auto"\n')
    monkeypatch.setenv("CLAUDE_PLUGIN_OPTION_MODE", "notify")
    c = load_config(p)
    assert c.mode == "notify"


def test_file_value_used_when_env_var_missing(monkeypatch, tmp_path):
    monkeypatch.delenv("CLAUDE_PLUGIN_OPTION_MODE", raising=False)
    p = tmp_path / "c.toml"
    p.write_text('[general]\nmode = "auto"\n')
    c = load_config(p)
    assert c.mode == "auto"


def test_invalid_env_var_raises(monkeypatch, tmp_path):
    """환경변수 값이 valid mode가 아니면 ValueError."""
    monkeypatch.setenv("CLAUDE_PLUGIN_OPTION_MODE", "garbage")
    with pytest.raises(ValueError, match="mode"):
        load_config(tmp_path / "missing.toml")


def test_empty_env_var_ignored(monkeypatch, tmp_path):
    """빈 문자열 환경변수는 무시 (override 없음 = 파일/기본값 사용)."""
    monkeypatch.setenv("CLAUDE_PLUGIN_OPTION_MODE", "")
    p = tmp_path / "c.toml"
    p.write_text('[general]\nmode = "auto"\n')
    c = load_config(p)
    assert c.mode == "auto"
```

### Step 2.2: 테스트 실행 — 실패 확인

Run: `pytest tests/lib/test_config_env_override.py -v`

Expected: 5 FAIL (env override 미구현).

### Step 2.3: `lib/config.py` 수정

Modify `lib/config.py` `load_config` 함수 — 환경변수 override 로직 추가:

```python
import os
import tomllib
from dataclasses import dataclass, field
from pathlib import Path
from typing import Literal

VALID_MODES = ("notify", "auto", "hybrid")

# (RefreshConfig / NotifyConfig / AdvancedConfig / Config dataclass 는 기존 그대로)


def _env_mode_override() -> str | None:
    """Claude Code userConfig 가 주입하는 환경변수.

    빈 문자열은 무시 (= override 없음). 잘못된 값은 호출자가 검증.
    """
    v = os.environ.get("CLAUDE_PLUGIN_OPTION_MODE")
    return v if v else None


def load_config(path: Path) -> Config:
    if path.exists():
        with open(path, "rb") as f:
            data = tomllib.load(f)
    else:
        data = {}
    general = data.get("general", {})
    mode = _env_mode_override() or general.get("mode", "hybrid")
    if mode not in VALID_MODES:
        raise ValueError(f"invalid mode: {mode}. Must be one of {VALID_MODES}")
    return Config(
        mode=mode,
        refresh_interval_minutes=general.get("refresh_interval_minutes", 55),
        max_refresh_count=general.get("max_refresh_count", 10),
        refresh=RefreshConfig(**data.get("refresh", {})),
        notify=NotifyConfig(**data.get("notify", {})),
        advanced=AdvancedConfig(**data.get("advanced", {})),
    )
```

### Step 2.4: 테스트 재실행 — 통과 + 기존 test_config.py 회귀 없음

Run: `pytest tests/lib/test_config.py tests/lib/test_config_env_override.py -v`

Expected: all passed (기존 test_config.py 의 테스트도 환경변수 없는 환경에서 그대로 통과).

### Step 2.5: Commit

```bash
git add lib/config.py tests/lib/test_config_env_override.py
git commit -m "feat: CLAUDE_PLUGIN_OPTION_MODE 환경변수로 mode override (userConfig 연동 준비)"
```

---

## Task 3: config.toml 자동 생성

**Files:**
- Modify: `lib/config.py` (헬퍼 함수 1개 추가)
- Create: `tests/lib/test_config_auto_create.py`
- Modify: `daemon/__main__.py`

### Step 3.1: 실패 테스트

Create `tests/lib/test_config_auto_create.py`:

```python
import pytest
from pathlib import Path
from lib.config import ensure_config_file


def test_creates_default_when_missing(tmp_path):
    path = tmp_path / "config.toml"
    ensure_config_file(path)
    assert path.exists()
    content = path.read_text()
    assert "[general]" in content
    assert "mode" in content
    assert "refresh_interval_minutes" in content


def test_does_not_overwrite_existing(tmp_path):
    """이미 있으면 절대 덮어쓰지 않음 (사용자 편집 보존)."""
    path = tmp_path / "config.toml"
    path.write_text("# my custom config\nmode = \"auto\"\n")
    ensure_config_file(path)
    assert path.read_text() == "# my custom config\nmode = \"auto\"\n"


def test_uses_env_mode_in_default_template(tmp_path, monkeypatch):
    """첫 생성 시 환경변수 mode 값을 템플릿에 반영."""
    monkeypatch.setenv("CLAUDE_PLUGIN_OPTION_MODE", "notify")
    path = tmp_path / "config.toml"
    ensure_config_file(path)
    content = path.read_text()
    assert 'mode = "notify"' in content


def test_default_template_when_env_missing(tmp_path, monkeypatch):
    monkeypatch.delenv("CLAUDE_PLUGIN_OPTION_MODE", raising=False)
    path = tmp_path / "config.toml"
    ensure_config_file(path)
    assert 'mode = "hybrid"' in path.read_text()


def test_invalid_env_mode_falls_back_to_default(tmp_path, monkeypatch):
    """잘못된 환경변수 값은 템플릿 작성 시 무시 (load_config 가 별도로 raise)."""
    monkeypatch.setenv("CLAUDE_PLUGIN_OPTION_MODE", "garbage")
    path = tmp_path / "config.toml"
    ensure_config_file(path)
    assert 'mode = "hybrid"' in path.read_text()


def test_created_file_has_0600_permission(tmp_path):
    import stat
    path = tmp_path / "config.toml"
    ensure_config_file(path)
    mode = stat.S_IMODE(path.stat().st_mode)
    assert mode == 0o600
```

### Step 3.2: 테스트 실행 — 실패 확인

Run: `pytest tests/lib/test_config_auto_create.py -v`

Expected: 6 FAIL (`ImportError: cannot import name 'ensure_config_file'`).

### Step 3.3: `lib/config.py` 에 `ensure_config_file` 추가

`lib/config.py` 끝에 추가:

```python
_DEFAULT_TEMPLATE = """# cache-necromancer 설정 — `/cn:config` 로 모드 비교 가능
# 이 파일은 첫 데몬 spawn 시 자동 생성됨. 수정 후 데몬 재시작 필요.

[general]
mode = "{mode}"                       # notify | auto | hybrid
refresh_interval_minutes = 55
max_refresh_count = 10

[refresh]
prompt = "."
hybrid_wait_seconds = 60
fire_timeout_seconds = 120

[notify]
terminal_bell = true
system_notification = true
imminent_threshold_minutes = 5

# [advanced] — 고급 옵션은 docs/superpowers/specs/...-design-v5.md 참조
"""


def ensure_config_file(path: Path) -> None:
    """파일이 없으면 기본 템플릿 작성. 있으면 그대로 둔다 (사용자 편집 보존).

    템플릿의 [general].mode 는 환경변수 ``CLAUDE_PLUGIN_OPTION_MODE`` 가
    valid mode 일 때만 그 값으로, 아니면 ``hybrid`` 로 채운다.
    """
    if path.exists():
        return
    env_mode = _env_mode_override()
    mode = env_mode if env_mode in VALID_MODES else "hybrid"
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(_DEFAULT_TEMPLATE.format(mode=mode))
    try:
        os.chmod(path, 0o600)
    except OSError:
        pass
```

### Step 3.4: 테스트 재실행 — 통과 확인

Run: `pytest tests/lib/test_config_auto_create.py -v`

Expected: 6 passed.

### Step 3.5: `daemon/__main__.py` 에서 호출

Modify `daemon/__main__.py`:

```python
import os
import sys
from pathlib import Path

from lib.config import ensure_config_file, load_config
from lib.lockfile import acquire_daemon_lock
from lib.logger import log_info, log_warn

from daemon.poller import run_poll_loop


def _resolve_root() -> Path:
    root = os.environ.get("CN_ROOT")
    if root:
        return Path(root)
    return Path.home() / ".cache-necromancer"


def main() -> int:
    root = _resolve_root()
    root.mkdir(parents=True, exist_ok=True)
    try:
        os.chmod(root, 0o700)
    except OSError:
        pass

    config_path = root / "config.toml"
    ensure_config_file(config_path)
    config = load_config(config_path)

    lock_path = root / "daemon.lock"
    lock_f = acquire_daemon_lock(lock_path)
    if lock_f is None:
        log_info("[daemon] another daemon alive, exit")
        return 0

    try:
        run_poll_loop(config)
    except KeyboardInterrupt:
        log_info("[daemon] interrupted, shutting down")
    except Exception as e:
        log_warn(f"[daemon] unexpected error: {type(e).__name__}: {e}")
        return 1
    finally:
        try:
            lock_f.close()
        except OSError:
            pass
        try:
            lock_path.unlink(missing_ok=True)
        except OSError:
            pass

    return 0


if __name__ == "__main__":
    sys.exit(main())
```

### Step 3.6: Commit

```bash
git add lib/config.py daemon/__main__.py tests/lib/test_config_auto_create.py
git commit -m "feat: 첫 데몬 spawn 시 config.toml 기본 템플릿 자동 생성 (편집 보존)"
```

---

## Task 4: plugin.json userConfig + 버전 bump

**Files:**
- Modify: `.claude-plugin/plugin.json`
- Modify: `.claude-plugin/marketplace.json`
- Modify: `pyproject.toml`

### Step 4.1: `plugin.json` — userConfig 1개 추가

Modify `.claude-plugin/plugin.json`:

```json
{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "cache-necromancer",
  "version": "0.1.1",
  "description": "Auto-refresh Claude Code prompt cache TTL via headless CLI",
  "author": {"name": "Brody Byun"},
  "license": "MIT",
  "keywords": ["cache", "cost", "macos"],
  "userConfig": {
    "mode": {
      "type": "string",
      "description": "동작 모드: notify (알림만, fire 없음) / auto (자동 fire) / hybrid (60초 사전 알림 후 fire, 취소 가능)",
      "default": "hybrid",
      "enum": ["notify", "auto", "hybrid"]
    }
  }
}
```

### Step 4.2: `marketplace.json` — version bump

Modify `.claude-plugin/marketplace.json` — `plugins[0].version` 을 `"0.1.0"` → `"0.1.1"` 로 교체.

### Step 4.3: `pyproject.toml` — version bump

Modify `pyproject.toml` — `version = "0.1.0"` → `version = "0.1.1"`.

### Step 4.4: 수동 검증 (사용자 직접)

이 step 은 자동화할 수 없음 — 사용자가 실행:

1. `/plugin uninstall cache-necromancer`
2. `/plugin marketplace update cache-necromancer-marketplace` (또는 remove + add)
3. `/plugin install cache-necromancer@cache-necromancer-marketplace`
4. 설치 시 `mode` 프롬프트가 뜨고 `notify` 선택
5. `/reload-plugins`
6. 짧은 응답 한 번 받기 (Stop hook → 데몬 spawn → config.toml 생성)
7. `cat ~/.cache-necromancer/config.toml` 에서 `mode = "notify"` 확인
8. `/cn:status` 에서 `현재 모드: 🔔 notify` 표시 확인

검증 통과 시 사용자가 확인 응답.

### Step 4.5: Commit

```bash
git add .claude-plugin/plugin.json .claude-plugin/marketplace.json pyproject.toml
git commit -m "feat: plugin.json userConfig (mode) + v0.1.1 bump"
```

---

## Task 5: README + CHANGELOG

**Files:**
- Modify: `README.md`
- Modify: `CHANGELOG.md`

### Step 5.1: README — 설치 섹션 보완

Modify `README.md` 의 "설치" 섹션 (line 30-40 부근). 다음 내용으로 교체:

```markdown
## 설치

```bash
# 마켓플레이스 (출시 후)
/plugin install cache-necromancer

# 로컬 marketplace (현재)
/plugin marketplace add /path/to/cache-necromancer
/plugin install cache-necromancer@cache-necromancer-marketplace
```

설치 중 `mode` 프롬프트가 나오면 처음엔 **`notify`** 권장 (실제 fire 없이 알림만으로 동작 확인). 설치 후 `/reload-plugins` 한 번 + 짧은 응답 한 번 받으면 데몬이 spawn 되고 `~/.cache-necromancer/config.toml` 이 자동 생성됨.
```

### Step 5.2: README — `/cn:status` 출력 예시 마스킹 반영

기존 출력 예시(line 102):

```
  abc12345 (this)
```

→ 이미 8자 마스크라 변경 불필요. 다만 일관성 위해 sid 표시 모두 8자 prefix로 유지되어 있는지 확인.

### Step 5.3: CHANGELOG `[0.1.1]` 섹션 추가

Modify `CHANGELOG.md` — `[0.1.0]` 섹션 위에 새로 추가:

```markdown
## [0.1.1] — 2026-05-13

v0.1.0 alpha 도그푸딩 첫 라운드에서 발견된 onboarding 마찰 4건 보완.

### Added

- **`userConfig` (mode)** — `/plugin install` 시점에 모드(notify/auto/hybrid) 선택 프롬프트.
- **config.toml 자동 생성** — 첫 데몬 spawn 시 `~/.cache-necromancer/config.toml` 이 없으면 기본 템플릿 생성. 이미 있으면 절대 덮어쓰지 않음 (사용자 편집 보존).
- **`CLAUDE_PLUGIN_OPTION_MODE` 환경변수 override** — Claude Code 공식 userConfig 메커니즘. 환경변수 > config.toml > 코드 기본값 순.

### Fixed

- **`/cn:dry-run` 과 `/cn:status` 출력에서 UUID 형식 session_id 원본 노출** — `sid_hash[:8]` 짧은 마스크로 표시 (transcript 캡처 노출 보호). 파일시스템 / 상태 비교는 원본 sid_hash 유지.
```

### Step 5.4: Commit

```bash
git add README.md CHANGELOG.md
git commit -m "docs: README 설치 절차 + CHANGELOG [0.1.1] 섹션"
```

---

## Task 6: 통합 검증 + PR 생성

### Step 6.1: 전체 테스트 통과 확인

Run: `pytest -v`

Expected: 기존 모든 테스트 + 신규 테스트 모두 통과.

### Step 6.2: 수동 검증 시나리오 (사용자 직접)

Task 4.4 의 8단계 시나리오를 사용자가 직접 실행하고 결과 보고.

### Step 6.3: 코드리뷰 (서브에이전트 병렬)

CLAUDE.md 코드리뷰 룰 따라 다음 7개 에이전트 병렬:
- 아키텍처 / 원칙 준수 / 중복-복잡도 / 사이드이펙트-에러 / 보안 / 성능 / 테스트 커버리지

CRITICAL 0건일 때만 다음 단계. 사용자에게 결과 보고.

### Step 6.4: 사용자 승인 받기

리뷰 결과 + 변경 요약 사용자에게 보고. 승인 받은 후 진행.

### Step 6.5: PR 생성

```bash
git push -u origin feature/v0.1.1-onboarding-polish
gh pr create --title "v0.1.1: onboarding polish (userConfig + sid 마스킹 + config 자동 생성)" --body "$(cat <<'EOF'
## Summary

v0.1.0 alpha 도그푸딩 첫 라운드 발견 onboarding 마찰 4건 보완:

- `/cn:dry-run`/`/cn:status` 출력의 UUID 원본 노출 → `sid_hash[:8]` 마스크 (transcript 노출 보호, CHANGELOG 약속 형식 충족)
- PRD 약속 미이행이었던 `userConfig` 도입 (mode 1개)
- 첫 spawn 시 `~/.cache-necromancer/config.toml` 기본 템플릿 자동 생성 (사용자 편집 보존)
- `CLAUDE_PLUGIN_OPTION_MODE` 환경변수 → config.toml mode override (Claude Code 공식 패턴)

PRD: `docs/superpowers/specs/2026-05-12-cache-necromancer-PRD.md` (line 281)
PLAN: `docs/superpowers/plans/2026-05-13-v0.1.1-onboarding-polish.md`

## Test plan

- [x] `pytest` 전체 통과 (신규 테스트 17개 포함)
- [x] `/plugin uninstall` → re-install 시 mode 프롬프트 표시 + 선택값이 config.toml 에 반영
- [x] `/cn:status` 출력에 UUID 원본 sid 노출 없음 + `(this)` 마커 정상
- [x] 기존 `config.toml` 있는 환경에서 자동 생성이 덮어쓰지 않음

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

### Step 6.6: 사용자 머지 승인 대기

CLAUDE.md 룰 — 머지는 사용자가 명시적으로 "머지해" 라고 지시할 때만.

---

## 자체 리뷰 (PLAN 작성 후 self-check)

- [x] **Spec coverage**:
  - 이슈 1 (sid 마스킹) → Task 1
  - 이슈 2 (userConfig 미구현) → Task 4
  - 이슈 3 (config.toml 자동 생성 안 됨) → Task 3
  - 이슈 4 (`/cn:config` 가 값 변경 못 함) → 의도된 단순화로 보고 PLAN 범위에서 제외 (CHANGELOG 에 한 줄 언급도 안 함, 추후 별도 결정).
  - 추가: env override (Task 2) — userConfig 의 동작 매개체.
- [x] **Placeholder scan**: TBD/TODO 없음.
- [x] **Type consistency**: `mask_sid`, `ensure_config_file`, `_env_mode_override`, `CLAUDE_PLUGIN_OPTION_MODE` 명명 모든 task 일관.
- [x] **Task 독립성**: Task 1 (마스킹) → Task 2 (env override) → Task 3 (자동 생성, env override 활용) → Task 4 (plugin.json, env override 진입점) → Task 5 (문서) → Task 6 (검증/PR). 의존 방향 단방향.

---

## 사용자 검토 체크리스트

PLAN OK 하실 때 다음 항목 확인:

1. **결정 D1~D6** 모두 동의?
2. **userConfig 범위 — `mode` 1개만** OK? 추가 노출 필요한 옵션 있다면 지금 결정.
3. **Task 1~6 분해 + TDD step** 적절한가?
4. **`/cn:config` 는 v0.1.1 범위에서 제외** OK? (별도 차후 결정)
5. **수동 검증 시나리오 (Task 4.4 + 6.2)** 사용자가 직접 진행하는 것 OK?
